---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: spireservers.operator.openshift.io
spec:
  group: operator.openshift.io
  names:
    kind: SpireServer
    listKind: SpireServerList
    plural: spireservers
    singular: spireserver
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SpireServer defines the configuration for the SPIRE Server managed by zero trust workload identity manager.
          This includes details related to trust domain, data storage, plugins
          and other configs required for workload authentication.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SpireServerSpec defines the specifications for configuring
              the SPIRE server.
            properties:
              additionalNodeAttestors:
                description: |-
                  additionalNodeAttestors configures additional NodeAttestor plugins for
                  this SPIRE server beyond the default k8s_psat. Use this when the server
                  acts as an upstream for nested SPIRE and needs to attest downstream
                  agents that use x509pop attestation.
                properties:
                  x509pop:
                    description: |-
                      x509pop enables the x509pop NodeAttestor on this SPIRE server. This
                      allows the server to attest agents from downstream clusters that present
                      a certificate signed by the specified CA. Set this on the upstream/hub
                      cluster's SpireServer CR.
                    properties:
                      caBundleSecretRef:
                        description: |-
                          caBundleSecretRef references a Secret containing the CA certificate
                          used to verify x509pop agent certificates from downstream clusters.
                          The Secret must be in the operator namespace.
                        properties:
                          key:
                            description: key is the key within the Secret data.
                            minLength: 1
                            type: string
                          name:
                            description: name is the name of the Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    required:
                    - caBundleSecretRef
                    type: object
                type: object
              affinity:
                description: |-
                  affinity defines scheduling affinity rules.
                  ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/
                properties:
                  nodeAffinity:
                    description: Describes node affinity scheduling rules for the
                      pod.
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and adding
                          "weight" to the sum if the node matches the corresponding matchExpressions; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: |-
                            An empty preferred scheduling term matches all objects with implicit weight 0
                            (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                          properties:
                            preference:
                              description: A node selector term, associated with the
                                corresponding weight.
                              properties:
                                matchExpressions:
                                  description: A list of node selector requirements
                                    by node's labels.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchFields:
                                  description: A list of node selector requirements
                                    by node's fields.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                              x-kubernetes-map-type: atomic
                            weight:
                              description: Weight associated with matching the corresponding
                                nodeSelectorTerm, in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - preference
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to an update), the system
                          may or may not try to eventually evict the pod from its node.
                        properties:
                          nodeSelectorTerms:
                            description: Required. A list of node selector terms.
                              The terms are ORed.
                            items:
                              description: |-
                                A null or empty node selector term matches no objects. The requirements of
                                them are ANDed.
                                The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                              properties:
                                matchExpressions:
                                  description: A list of node selector requirements
                                    by node's labels.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchFields:
                                  description: A list of node selector requirements
                                    by node's fields.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                              x-kubernetes-map-type: atomic
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - nodeSelectorTerms
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  podAffinity:
                    description: Describes pod affinity scheduling rules (e.g. co-locate
                      this pod in the same node, zone, etc. as some other pod(s)).
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and adding
                          "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: The weights of all of the matched WeightedPodAffinityTerm
                            fields are added per-node to find the most preferred node(s)
                          properties:
                            podAffinityTerm:
                              description: Required. A pod affinity term, associated
                                with the corresponding weight.
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            weight:
                              description: |-
                                weight associated with matching the corresponding podAffinityTerm,
                                in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - podAffinityTerm
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to a pod label update), the
                          system may or may not try to eventually evict the pod from its node.
                          When there are multiple elements, the lists of nodes corresponding to each
                          podAffinityTerm are intersected, i.e. all terms must be satisfied.
                        items:
                          description: |-
                            Defines a set of pods (namely those matching the labelSelector
                            relative to the given namespace(s)) that this pod should be
                            co-located (affinity) or not co-located (anti-affinity) with,
                            where co-located is defined as running on a node whose value of
                            the label with key <topologyKey> matches that of any node on which
                            a pod of the set of pods is running
                          properties:
                            labelSelector:
                              description: |-
                                A label query over a set of resources, in this case pods.
                                If it's null, this PodAffinityTerm matches with no Pods.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            matchLabelKeys:
                              description: |-
                                MatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                Also, matchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            mismatchLabelKeys:
                              description: |-
                                MismatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaceSelector:
                              description: |-
                                A label query over the set of namespaces that the term applies to.
                                The term is applied to the union of the namespaces selected by this field
                                and the ones listed in the namespaces field.
                                null selector and null or empty namespaces list means "this pod's namespace".
                                An empty selector ({}) matches all namespaces.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: |-
                                namespaces specifies a static list of namespace names that the term applies to.
                                The term is applied to the union of the namespaces listed in this field
                                and the ones selected by namespaceSelector.
                                null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            topologyKey:
                              description: |-
                                This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                whose value of the label with key topologyKey matches that of any node on which any of the
                                selected pods is running.
                                Empty topologyKey is not allowed.
                              type: string
                          required:
                          - topologyKey
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                  podAntiAffinity:
                    description: Describes pod anti-affinity scheduling rules (e.g.
                      avoid putting this pod in the same node, zone, etc. as some
                      other pod(s)).
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the anti-affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling anti-affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and subtracting
                          "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: The weights of all of the matched WeightedPodAffinityTerm
                            fields are added per-node to find the most preferred node(s)
                          properties:
                            podAffinityTerm:
                              description: Required. A pod affinity term, associated
                                with the corresponding weight.
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            weight:
                              description: |-
                                weight associated with matching the corresponding podAffinityTerm,
                                in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - podAffinityTerm
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the anti-affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the anti-affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to a pod label update), the
                          system may or may not try to eventually evict the pod from its node.
                          When there are multiple elements, the lists of nodes corresponding to each
                          podAffinityTerm are intersected, i.e. all terms must be satisfied.
                        items:
                          description: |-
                            Defines a set of pods (namely those matching the labelSelector
                            relative to the given namespace(s)) that this pod should be
                            co-located (affinity) or not co-located (anti-affinity) with,
                            where co-located is defined as running on a node whose value of
                            the label with key <topologyKey> matches that of any node on which
                            a pod of the set of pods is running
                          properties:
                            labelSelector:
                              description: |-
                                A label query over a set of resources, in this case pods.
                                If it's null, this PodAffinityTerm matches with no Pods.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            matchLabelKeys:
                              description: |-
                                MatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                Also, matchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            mismatchLabelKeys:
                              description: |-
                                MismatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaceSelector:
                              description: |-
                                A label query over the set of namespaces that the term applies to.
                                The term is applied to the union of the namespaces selected by this field
                                and the ones listed in the namespaces field.
                                null selector and null or empty namespaces list means "this pod's namespace".
                                An empty selector ({}) matches all namespaces.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: |-
                                namespaces specifies a static list of namespace names that the term applies to.
                                The term is applied to the union of the namespaces listed in this field
                                and the ones selected by namespaceSelector.
                                null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            topologyKey:
                              description: |-
                                This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                whose value of the label with key topologyKey matches that of any node on which any of the
                                selected pods is running.
                                Empty topologyKey is not allowed.
                              type: string
                          required:
                          - topologyKey
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                type: object
              caKeyType:
                default: rsa-2048
                description: |-
                  caKeyType specifies the key type used for the server CA (both X509 and JWT).
                  Valid values are: rsa-2048, rsa-4096, ec-p256, ec-p384.
                enum:
                - rsa-2048
                - rsa-4096
                - ec-p256
                - ec-p384
                type: string
              caSubject:
                description: caSubject contains subject information for the SPIRE
                  CA.
                properties:
                  commonName:
                    description: |-
                      commonName specifies the common name for the CA.
                      Must contain only alphanumeric characters, spaces, dots, underscores, or hyphens.
                    maxLength: 255
                    pattern: ^[a-zA-Z0-9 ._-]*$
                    type: string
                  country:
                    description: |-
                      country specifies the country for the CA.
                      ISO 3166-1 alpha-2 country code (2 characters).
                    maxLength: 2
                    type: string
                  organization:
                    description: organization specifies the organization for the CA.
                    maxLength: 64
                    type: string
                type: object
              caValidity:
                default: 24h
                description: |-
                  caValidity is the validity period (TTL) for the SPIRE Server's own CA certificate.
                  This determines how long the server's root or intermediate certificate is valid.
                format: duration
                type: string
              datastore:
                description: datastore configures the SPIRE server SQL datastore backend.
                properties:
                  connMaxLifetime:
                    description: |-
                      connMaxLifetime specifies the maximum lifetime of a database connection in seconds.
                      A value of 0 means connections are not closed due to age.
                    minimum: 0
                    type: integer
                  connectionString:
                    default: /run/spire/data/datastore.sqlite3
                    description: |-
                      connectionString contains connection credentials required for the SPIRE server datastore.
                      Must not be empty and should contain valid connection parameters for the specified database type.
                      For PostgreSQL with SSL, include sslmode and certificate paths in the connection string.
                      Example: "dbname=spire user=spire host=postgres.example.com sslmode=verify-full sslrootcert=/run/spire/db/certs/ca.crt"
                    maxLength: 2048
                    minLength: 1
                    type: string
                  databaseType:
                    default: sqlite3
                    description: databaseType specifies type of database to use.
                    enum:
                    - sql
                    - sqlite3
                    - postgres
                    - mysql
                    - aws_postgresql
                    - aws_mysql
                    type: string
                  disableMigration:
                    default: "false"
                    description: |-
                      disableMigration specifies the migration state
                      If true, disables DB auto-migration.
                    enum:
                    - "true"
                    - "false"
                    type: string
                  maxIdleConns:
                    default: 2
                    description: |-
                      maxIdleConns specifies the maximum number of idle database connections.
                      Must be between 0 and 10000.
                    maximum: 10000
                    minimum: 0
                    type: integer
                  maxOpenConns:
                    default: 100
                    description: |-
                      DB pool config
                      maxOpenConns specifies the maximum number of open database connections.
                      Must be between 1 and 10000.
                    maximum: 10000
                    minimum: 1
                    type: integer
                  tlsSecretName:
                    description: |-
                      tlsSecretName specifies the name of a Kubernetes Secret containing TLS certificates for database connections.
                      The Secret will be mounted at /run/spire/db/certs in the SPIRE server container.
                      The Secret should contain keys like 'ca.crt', 'tls.crt', 'tls.key' for the respective certificates.
                      For PostgreSQL, reference these certificates in the connectionString, e.g.:
                      "sslmode=verify-full sslrootcert=/run/spire/db/certs/ca.crt sslcert=/run/spire/db/certs/tls.crt sslkey=/run/spire/db/certs/tls.key"
                    type: string
                required:
                - connectionString
                - databaseType
                type: object
              defaultJWTValidity:
                default: 5m
                description: |-
                  defaultJWTValidity is the default validity period (TTL) for JWT SVIDs issued to workloads.
                  This value is used if a specific TTL is not configured for a registration entry.
                format: duration
                type: string
              defaultX509Validity:
                default: 1h
                description: |-
                  defaultX509Validity is the default validity period (TTL) for X.509 SVIDs issued to workloads.
                  This value is used if a specific TTL is not configured for a registration entry.
                format: duration
                type: string
              exportGRPCRoute:
                default: false
                description: |-
                  exportGRPCRoute controls whether the operator creates a passthrough Route
                  to expose this SPIRE server's gRPC API (port 8081) for cross-cluster
                  nested SPIRE. When true, downstream clusters can reach this server via
                  the Route on port 443. Set this to true on the upstream/hub cluster's
                  SpireServer CR so downstream spoke clusters can connect their
                  upstream-agent sidecars to it.
                type: boolean
              federation:
                description: federation configures SPIRE federation endpoints and
                  relationships
                properties:
                  bundleEndpoint:
                    description: bundleEndpoint configures this cluster's federation
                      bundle endpoint
                    properties:
                      httpsWeb:
                        description: httpsWeb configures the https_web profile (required
                          if profile is https_web)
                        properties:
                          acme:
                            description: |-
                              acme configures automatic certificate management using ACME protocol
                              Mutually exclusive with servingCert
                            properties:
                              directoryUrl:
                                description: directoryUrl is the ACME directory URL
                                  (e.g., Let's Encrypt)
                                pattern: ^https://.*
                                type: string
                              domainName:
                                description: domainName is the domain name for the
                                  certificate
                                type: string
                              email:
                                description: email for ACME account registration
                                pattern: ^[a-zA-Z0-9][a-zA-Z0-9._%+-]*[a-zA-Z0-9]@[a-zA-Z0-9]([a-zA-Z0-9-]*[a-zA-Z0-9])?(\.[a-zA-Z0-9]([a-zA-Z0-9-]*[a-zA-Z0-9])?)*\.[a-zA-Z]{2,}$
                                type: string
                              tosAccepted:
                                default: "false"
                                description: tosAccepted indicates acceptance of Terms
                                  of Service
                                enum:
                                - "true"
                                - "false"
                                type: string
                            required:
                            - directoryUrl
                            - domainName
                            - email
                            type: object
                          servingCert:
                            description: |-
                              servingCert configures certificate from a Kubernetes Secret
                              Mutually exclusive with acme
                            properties:
                              externalSecretRef:
                                description: |-
                                  externalSecretRef is a reference to an externally managed secret that contains
                                  the TLS certificate for the SPIRE server federation Route host. The secret must
                                  be in the same namespace where the operator and operands are deployed and must
                                  contain tls.crt and tls.key fields. The OpenShift Ingress Operator will read
                                  this secret to configure the route's TLS certificate.
                                type: string
                              fileSyncInterval:
                                default: 86400
                                description: fileSyncInterval is how often to check
                                  for certificate updates (seconds)
                                format: int32
                                maximum: 7776000
                                minimum: 3600
                                type: integer
                            type: object
                        type: object
                        x-kubernetes-validations:
                        - message: exactly one of acme or servingCert must be set
                          rule: (has(self.acme) && !has(self.servingCert)) || (!has(self.acme)
                            && has(self.servingCert))
                        - message: cannot switch from acme to servingCert configuration
                          rule: '!has(oldSelf.acme) || has(self.acme)'
                        - message: cannot switch from servingCert to acme configuration
                          rule: '!has(oldSelf.servingCert) || has(self.servingCert)'
                      profile:
                        allOf:
                        - enum:
                          - https_spiffe
                          - https_web
                        - enum:
                          - https_spiffe
                          - https_web
                        default: https_spiffe
                        description: profile is the bundle endpoint authentication
                          profile
                        type: string
                      refreshHint:
                        default: 300
                        description: refreshHint is the hint for bundle refresh interval
                          in seconds
                        format: int32
                        maximum: 3600
                        minimum: 60
                        type: integer
                    required:
                    - profile
                    type: object
                    x-kubernetes-validations:
                    - message: httpsWeb is required when profile is https_web
                      rule: 'self.profile == ''https_web'' ? has(self.httpsWeb) :
                        true'
                    - message: profile is immutable and cannot be changed once set
                      rule: '!has(oldSelf.profile) || oldSelf.profile == self.profile'
                  federatesWith:
                    description: federatesWith lists trust domains this cluster federates
                      with
                    items:
                      description: FederatesWithConfig represents a remote trust domain
                        to federate with
                      properties:
                        bundleEndpointProfile:
                          allOf:
                          - enum:
                            - https_spiffe
                            - https_web
                          - enum:
                            - https_spiffe
                            - https_web
                          description: bundleEndpointProfile is the authentication
                            profile of the remote endpoint
                          type: string
                        bundleEndpointUrl:
                          description: bundleEndpointUrl is the URL of the remote
                            federation endpoint
                          pattern: ^https://.*
                          type: string
                        endpointSpiffeId:
                          description: endpointSpiffeId is required for https_spiffe
                            profile
                          pattern: ^spiffe://.*
                          type: string
                        trustDomain:
                          description: trustDomain is the federated trust domain name
                          pattern: ^[a-z0-9._-]{1,255}$
                          type: string
                      required:
                      - bundleEndpointProfile
                      - bundleEndpointUrl
                      - trustDomain
                      type: object
                      x-kubernetes-validations:
                      - message: endpointSpiffeId is required when bundleEndpointProfile
                          is https_spiffe
                        rule: 'self.bundleEndpointProfile == ''https_spiffe'' ? has(self.endpointSpiffeId)
                          && self.endpointSpiffeId != '''' : true'
                    maxItems: 50
                    minItems: 1
                    type: array
                  managedRoute:
                    default: "true"
                    description: |-
                      managedRoute enables or disables automatic Route creation for the federation endpoint
                      "true": Allows automatic exposure of federation endpoint through a managed OpenShift Route.
                      "false": Allows administrators to manually configure exposure using custom OpenShift Routes or ingress, offering more control over routing behavior.
                    enum:
                    - "true"
                    - "false"
                    type: string
                required:
                - bundleEndpoint
                type: object
              jwtIssuer:
                description: |-
                  jwtIssuer is the JWT issuer url.
                  Must be a valid HTTPS or HTTP URL.
                maxLength: 512
                pattern: ^(?i)https?://[^\s?#]+$
                type: string
              jwtKeyType:
                description: |-
                  jwtKeyType specifies the key type used for JWT signing.
                  Valid values are: rsa-2048, rsa-4096, ec-p256, ec-p384.
                  This field is optional and will only be set in the SPIRE server configuration if explicitly provided.
                enum:
                - rsa-2048
                - rsa-4096
                - ec-p256
                - ec-p384
                type: string
              keyManager:
                description: keyManager configures the SPIRE server key manager.
                properties:
                  diskEnabled:
                    default: "true"
                    description: diskEnabled enables the disk-based key manager.
                    enum:
                    - "true"
                    - "false"
                    type: string
                  memoryEnabled:
                    default: "false"
                    description: memoryEnabled enables the memory-based key manager
                    enum:
                    - "true"
                    - "false"
                    type: string
                type: object
              labels:
                additionalProperties:
                  type: string
                description: |-
                  labels to apply to all resources managed by the API.
                  Maximum 64 labels allowed. Label keys and values must be valid Kubernetes labels.
                maxProperties: 64
                type: object
                x-kubernetes-map-type: granular
              logFormat:
                default: text
                description: |-
                  logFormat sets the logging format for the operand.
                  Valid values are: text, json.
                enum:
                - text
                - json
                type: string
              logLevel:
                default: info
                description: |-
                  logLevel sets the logging level for the operand.
                  Valid values are: debug, info, warn, error.
                enum:
                - debug
                - info
                - warn
                - error
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                description: |-
                  nodeSelector defines the scheduling criteria using node labels.
                  Maximum 50 node selectors allowed.
                  ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/
                maxProperties: 50
                type: object
                x-kubernetes-map-type: atomic
              persistence:
                description: |-
                  persistence configures storage for the SPIRE server.
                  This field is required and immutable once set.
                properties:
                  accessMode:
                    default: ReadWriteOnce
                    description: accessMode for the volume.
                    enum:
                    - ReadWriteOnce
                    - ReadWriteOncePod
                    - ReadWriteMany
                    type: string
                  size:
                    default: 1Gi
                    description: size of the persistent volume (e.g., 1Gi).
                    pattern: ^[1-9][0-9]*Gi$
                    type: string
                  storageClass:
                    default: ""
                    description: storageClass to be used for the PVC.
                    type: string
                required:
                - accessMode
                - size
                type: object
              resources:
                description: |-
                  resources define the resource requirements.
                  ref: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              tolerations:
                description: |-
                  tolerations define the pod tolerations.
                  Maximum 50 tolerations allowed.
                  ref: https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/
                items:
                  description: |-
                    The pod this Toleration is attached to tolerates any taint that matches
                    the triple <key,value,effect> using the matching operator <operator>.
                  properties:
                    effect:
                      description: |-
                        Effect indicates the taint effect to match. Empty means match all taint effects.
                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: |-
                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                      type: string
                    operator:
                      description: |-
                        Operator represents a key's relationship to the value.
                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a pod can
                        tolerate all taints of a particular category.
                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                      type: string
                    tolerationSeconds:
                      description: |-
                        TolerationSeconds represents the period of time the toleration (which must be
                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                        negative values will be treated as 0 (evict immediately) by the system.
                      format: int64
                      type: integer
                    value:
                      description: |-
                        Value is the taint value the toleration matches to.
                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                      type: string
                  type: object
                maxItems: 50
                type: array
                x-kubernetes-list-type: atomic
              upstreamAuthority:
                description: |-
                  upstreamAuthority configures an external PKI to sign the SPIRE intermediate CA.
                  When absent, SPIRE uses a self-signed CA.
                properties:
                  certManager:
                    description: certManager configures the cert-manager UpstreamAuthority
                      plugin.
                    properties:
                      issuerGroup:
                        default: cert-manager.io
                        description: issuerGroup is the API group of the issuer.
                        maxLength: 253
                        minLength: 1
                        type: string
                      issuerKind:
                        default: Issuer
                        description: issuerKind is the kind of the issuer (Issuer
                          or ClusterIssuer).
                        enum:
                        - Issuer
                        - ClusterIssuer
                        type: string
                      issuerName:
                        description: issuerName is the name of the cert-manager Issuer
                          or ClusterIssuer.
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: namespace is the namespace to create CertificateRequests
                          in.
                        maxLength: 63
                        minLength: 1
                        type: string
                    required:
                    - issuerName
                    - namespace
                    type: object
                  spire:
                    description: |-
                      spire configures the nested SPIRE UpstreamAuthority plugin.
                      When set, the operator injects an upstream-agent sidecar into the
                      SPIRE server StatefulSet. This agent attests to a remote upstream
                      SPIRE server and exposes a Workload API socket that the downstream
                      SPIRE server uses to obtain its intermediate CA.
                    properties:
                      nodeAttestor:
                        description: |-
                          nodeAttestor configures the node attestation method used by the
                          upstream-agent sidecar to prove its identity to the upstream SPIRE server.
                          Exactly one attestation method must be configured.
                        properties:
                          k8sPsat:
                            description: |-
                              k8sPsat configures Kubernetes Projected Service Account Token attestation.
                              The sidecar presents a projected SA token to the upstream SPIRE server,
                              which validates it via the downstream cluster's TokenReview API.
                              Requires a kubeconfig Secret on the upstream cluster for cross-cluster
                              token validation.
                            properties:
                              clusterName:
                                description: |-
                                  clusterName is the cluster name that must match the cluster entry in
                                  the upstream SPIRE server's k8s_psat NodeAttestor configuration.
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - clusterName
                            type: object
                          x509pop:
                            description: |-
                              x509pop configures X.509 Certificate Proof of Possession attestation.
                              The sidecar presents a pre-provisioned certificate to prove its identity
                              to the upstream SPIRE server. Recommended for cross-cluster deployments
                              because it requires no cross-cluster API access and is reattestable.
                            properties:
                              certificateSecretName:
                                description: |-
                                  certificateSecretName is the name of a Secret in the operator namespace
                                  containing the x509pop agent certificate and private key. The Secret
                                  must contain two keys:
                                    - agent.crt: the agent certificate in PEM format (must include
                                      digitalSignature key usage)
                                    - agent.key: the agent private key in PEM format
                                  The certificate must be signed by a CA that the upstream SPIRE server
                                  trusts (configured in the upstream server's x509pop NodeAttestor
                                  ca_bundle_path).
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - certificateSecretName
                            type: object
                        type: object
                        x-kubernetes-validations:
                        - message: exactly one of x509pop or k8sPsat must be set
                          rule: '(has(self.x509pop) ? 1 : 0) + (has(self.k8sPsat)
                            ? 1 : 0) == 1'
                      trustBundle:
                        description: |-
                          trustBundle configures how the upstream-agent sidecar obtains the
                          upstream SPIRE server's trust bundle for initial TLS verification.
                        properties:
                          insecureBootstrap:
                            default: false
                            description: |-
                              insecureBootstrap enables Trust On First Use (TOFU) mode for the
                              upstream-agent sidecar. When true, the sidecar skips server certificate
                              verification on its first connection and pins the trust bundle received
                              from the server for subsequent connections.
                              WARNING: This is vulnerable to man-in-the-middle attacks on the first
                              connection. Use secretRef for production deployments.
                            type: boolean
                          secretRef:
                            description: |-
                              secretRef references a Secret containing the upstream SPIRE server's
                              root CA certificate used for TLS verification during initial connection.
                              The Secret must be in the operator namespace and contain the CA
                              certificate in PEM format.
                            properties:
                              key:
                                description: key is the key within the Secret data.
                                minLength: 1
                                type: string
                              name:
                                description: name is the name of the Secret.
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                        type: object
                        x-kubernetes-validations:
                        - message: either secretRef must be set or insecureBootstrap
                            must be true
                          rule: has(self.secretRef) || self.insecureBootstrap == true
                        - message: secretRef and insecureBootstrap are mutually exclusive
                          rule: '!has(self.secretRef) || self.insecureBootstrap !=
                            true'
                      upstreamServerAddress:
                        description: |-
                          upstreamServerAddress is the hostname of the upstream SPIRE server's
                          OpenShift Route (e.g., spire-server-ns.apps.hub-cluster.example.com).
                          The upstream-agent sidecar connects to this address to attest and
                          obtain its SVID.
                        maxLength: 512
                        minLength: 1
                        type: string
                      upstreamServerPort:
                        default: 443
                        description: |-
                          upstreamServerPort is the port of the upstream SPIRE server Route.
                          Use 443 when connecting through an OpenShift passthrough Route.
                          Use 8081 when connecting through a LoadBalancer or ClusterIP Service.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                    required:
                    - nodeAttestor
                    - trustBundle
                    - upstreamServerAddress
                    type: object
                  vault:
                    description: vault configures the HashiCorp Vault UpstreamAuthority
                      plugin.
                    properties:
                      caCertSecretRef:
                        description: |-
                          caCertSecretRef references a Secret containing the CA certificate
                          used to verify the Vault server's TLS certificate.
                        properties:
                          key:
                            description: key is the key within the Secret data.
                            minLength: 1
                            type: string
                          name:
                            description: name is the name of the Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      insecureSkipVerify:
                        default: false
                        description: insecureSkipVerify disables TLS certificate verification
                          for the Vault connection.
                        type: boolean
                      k8sAuth:
                        description: k8sAuth configures Kubernetes auth method for
                          Vault authentication.
                        properties:
                          audience:
                            default: vault
                            description: audience must match the bound_audiences configured
                              on the Vault role.
                            type: string
                          k8sAuthMountPoint:
                            default: kubernetes
                            description: k8sAuthMountPoint is the Vault auth mount
                              path for the Kubernetes auth method.
                            minLength: 1
                            type: string
                          k8sAuthRoleName:
                            description: k8sAuthRoleName is the Vault role bound to
                              the SPIRE server ServiceAccount.
                            minLength: 1
                            type: string
                        required:
                        - k8sAuthRoleName
                        type: object
                      pkiMountPoint:
                        default: pki
                        description: pkiMountPoint is the Vault mount path for the
                          PKI secrets engine.
                        minLength: 1
                        type: string
                      vaultAddr:
                        description: |-
                          vaultAddr is the URL of the Vault server (e.g., https://vault.example.org/).
                          HTTP is permitted for in-cluster Vault instances reached via the Kubernetes
                          service network; use HTTPS for any Vault endpoint reachable outside the cluster.
                        pattern: ^https?://.+
                        type: string
                      vaultNamespace:
                        description: vaultNamespace is the Vault Enterprise namespace
                          (leave empty for OSS Vault).
                        type: string
                    required:
                    - k8sAuth
                    - vaultAddr
                    type: object
                type: object
                x-kubernetes-validations:
                - message: exactly one of certManager, vault, or spire must be set
                  rule: '(has(self.certManager) ? 1 : 0) + (has(self.vault) ? 1 :
                    0) + (has(self.spire) ? 1 : 0) == 1'
            required:
            - caSubject
            - datastore
            - jwtIssuer
            - persistence
            type: object
          status:
            description: SpireServerStatus defines the observed state of the SPIRE
              server reconciliation performed by the operator.
            properties:
              conditions:
                description: conditions holds information about the current state
                  of the SPIRE resources deployment.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
        x-kubernetes-validations:
        - message: Federation configuration cannot be removed once set.
          rule: oldSelf == null || !has(oldSelf.spec.federation) || has(self.spec.federation)
        - message: SpireServer is a singleton, .metadata.name must be 'cluster'
          rule: self.metadata.name == 'cluster'
        - message: spec.persistence.size is immutable
          rule: oldSelf.spec.persistence.size == self.spec.persistence.size
        - message: spec.persistence.accessMode is immutable
          rule: oldSelf.spec.persistence.accessMode == self.spec.persistence.accessMode
        - message: spec.persistence.storageClass is immutable
          rule: oldSelf.spec.persistence.storageClass == self.spec.persistence.storageClass
    served: true
    storage: true
    subresources:
      status: {}
